# InCoVID Data Scrapper

#### Install requirements
`pip install -r requirements.txt` or `pip3 install -r requirements.txt`

#### Run the script
`python incovid_scrapper.py` or `python3 incovid_scrapper.py`
